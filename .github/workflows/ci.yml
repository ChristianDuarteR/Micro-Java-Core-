name: Java CI

on:
  push:
    branches:
      - main
      - master
      - develop
  pull_request:
    branches:
      - main
      - master
      - develop
    types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-and-build:
    name: Tests and Docker Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Java
        uses: actions/setup-java@3a4728e2025c27f3d45ef27271e892c9431e78eb # v4.8.0
        with:
          distribution: temurin
          java-version: "17"
          cache: maven

      - name: Run tests and coverage
        run: mvn -B verify

      - name: Build Docker image
        run: docker build -t global-invoice-java:${{ github.sha }} .

  sonarqube:
    name: SonarQube Analysis
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@3a4728e2025c27f3d45ef27271e892c9431e78eb # v4.8.0
        with:
          java-version: "17"
          distribution: "zulu"

      - name: Cache SonarQube packages
        uses: actions/cache@11d8d2e981b1dca3922f30b91e92d9d93652885d # v4.3.0
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      - name: Cache Maven packages
        uses: actions/cache@11d8d2e981b1dca3922f30b91e92d9d93652885d # v4.3.0
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      - name: Build and analyze
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: mvn -B verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -Dsonar.projectKey=ChristianDuarteR_Micro-Java-Core